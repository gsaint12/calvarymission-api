# To initialize this app, git clone this repository

run 
$ npm install to install all dependencies

# To use an ORM database tool like sequelize.
install sequelize globally:

$ npm install -g sequelize